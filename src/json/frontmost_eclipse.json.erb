{
    "title": "FrontmostIfVscode999",
    "rules": [
      {
        "description": "LeftOption + M_BookmarkSet(vscode)",
        "manipulators": [
          {
            "type": "basic",
            "from": <%= from("m", ["left_option"], ["caps_lock"]) %>,
            "to": <%= to([["k", ["right_option", "right_gui"]]]) %>,
            "conditions": [ <%= frontmost_application_if("vscode") %> ]
          }
        ]
      }
 ]
}
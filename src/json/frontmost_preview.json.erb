{
    "title": "FrontmostIfpreview",
    "rules": [
    {
      "description": "RightAlt + A_FullScreen(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("a", ["right_alt"]) %>,
          "to": <%= to([["7", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    },
    {
      "description": "LeftAlt + R_RowNumberSelectMove(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("r", ["left_alt"]) %>,
          "to": <%= to([["8", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    }
    ]
}
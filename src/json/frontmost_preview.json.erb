{
    "title": "FrontmostIfpreview1",
    "rules": [
    {
      "description": "LeftCommand + I_Expansion(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("i", ["left_control"]) %>,
          "to": <%= to([["equal_sign", ["left_shift", "right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + U_Shrink(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("u", ["left_control"]) %>,
          "to": <%= to([["hyphen", ["left_shift", "right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    },
    {
      "description": "LeftAlt + R_RowNumberSelectMove(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("r", ["left_alt"]) %>,
          "to": <%= to([["8", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + T_LeftSideBarToggleOnOff(preview)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("t", ["left_control"]) %>,
          "to": <%= to([["1", ["left_control", "right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("preview") %> ]
        }
      ]
    }
    ]
}
{
  "title": "eisukey_japanisekey(@sisui)",
  "rules": [
    {
      "description": "eisukey_japanisekey",
      "manipulators":
      <%=
               each_key(
                 source_keys_list: ["left_control", "right_command"],
                 dest_keys_list: ["semicolon", "j"],
                 to_modifiers: ["right_shift"],
                 to_if_alone: [{"key_code": "left_control"}],
                 as_json: true
               )
       %>
    }
    ]
}
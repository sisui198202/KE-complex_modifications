{
    "title": "FrontmostIfElectron5",
    "rules": [
      {
        "description": "RightCommand + H_Reload(electron)",
        "manipulators": [
          {
            "type": "basic",
            "from": <%= from("h", ["right_gui"], ["caps_lock"]) %>,
            "to": <%= to([["r", ["right_gui"]]]) %>,
            "conditions": [ <%= frontmost_application_if("electron") %> ]
          }
        ]
      }
    ]
}
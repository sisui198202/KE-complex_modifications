{
  "title": "Window Control",
  "rules": [
    {
      "description": "Fn + IJKL_WindowsControl",
      "manipulators": [
        {
          "description": "Fn + I_WindowsControl",
          "type": "basic",
          "from": <%= from("i", ["fn"]) %>,
          "to": <%= to([["6","right_alt"]]) %>
        },
    {
        "type": "basic",
        "from": <%= from("j", ["fn"]) %>,
        "to": <%= to([["8","right_alt"]]) %>
    },
    {
        "type": "basic",
        "from": <%= from("k", ["fn"]) %>,
        "to": <%= to([["7","right_alt"]]) %>
    },
    {
        "type": "basic",
        "from": <%= from("l", ["fn"]) %>,
        "to": <%= to([["9","right_alt"]]) %>
    }
 ]
}
  ]
}
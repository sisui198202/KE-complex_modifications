{
    "title": "FrontmostIfSkitch99",
    "rules": [
    {
      "description": "LeftOption + O(オー)_Export(skitch)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("o", ["left_option"]) %>,
          "to": <%= to([["e", ["right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("skitch") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + u_長方形(skitch)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("u", ["left_control"]) %>,
          "to": <%= to([["r", ["right_gui","right_option"]]]) %>,
          "conditions": [ <%= frontmost_application_if("skitch") %> ]
        }
      ]
    }
    ]
}
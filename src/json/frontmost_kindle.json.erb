{
    "title": "FrontmostIfkindle",
    "rules": [
    {
      "description": "RightAlt + A_FullScreen(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("a", ["right_alt"]) %>,
          "to": <%= to([["7", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    },
    {
      "description": "LeftAlt + R_RowNumberSelectMove(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("r", ["left_alt"]) %>,
          "to": <%= to([["8", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + Z_Home(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("z", ["left_control"]) %>,
          "to": <%= to([["9", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    },
    {
      "description": "RightAlt + Z_To_IndexPage(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("z", ["right_alt"]) %>,
          "to": <%= to([["4", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + I_Expansion(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("i", ["left_control"]) %>,
          "to": <%= to([["equal_sign", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    },
    {
      "description": "LeftCommand + U_Shrink(kindle)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("u", ["left_control"]) %>,
          "to": <%= to([["backslash", ["left_control", "left_alt"]]]) %>,
          "conditions": [ <%= frontmost_application_if("kindle") %> ]
        }
      ]
    }
    ]
}
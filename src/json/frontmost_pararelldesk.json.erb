{
    "title": "FrontmostIfPararell",
    "rules": [
    {
      "description": "LeftAlt + O(オー)_Save(Pararell)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("o", ["left_alt"], ["caps_lock"]) %>,
          "to": <%= to([["s", ["left_control"]]]) %>,
          "conditions": [ <%= frontmost_application_if("pararell") %> ]
        }
      ]
    }
   ]
}
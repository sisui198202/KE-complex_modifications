{
  "title": "FrontmostIfMail",
  "rules": [
    {
      "description": "LeftOption + D_日表示(mail)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("d", ["left_option"], ["caps_lock"]) %>,
          "to": <%= to([["1", ["right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("mail") %> ]
        }
      ]
    }
  ]
}
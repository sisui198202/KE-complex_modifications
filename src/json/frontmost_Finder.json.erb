{
    "title": "FrontmostIfFinder111",
    "rules": [
    {
      "description": "LeftOption + SpaceBar_FileOpen(Finder)",
      "manipulators": [
        {
          "type": "basic",
          "from": <%= from("spacebar", ["left_option"]) %>,
          "to": <%= to([["o", ["right_gui"]]]) %>,
          "conditions": [ <%= frontmost_application_if("finder") %> ]
        }
      ]
    }
    ]
}